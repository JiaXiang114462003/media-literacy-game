import React from 'react';

/**
 * 抬頭顯示器 (HUD) 元件
 * 用於顯示頂部的分數與時間條
 */
export default function HUD({ score, timeRemaining }) {
  return (
    <div className="hud">
      {/* HUD 內容將在此實現 */}
    </div>
  );
}
